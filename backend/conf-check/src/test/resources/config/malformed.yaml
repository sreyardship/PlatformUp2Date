# Fixture for YamlAppConfigReaderTests (issue 06): deliberately invalid YAML syntax (an unclosed
# flow-mapping bracket), used to assert AppConfigReader.ConfigReadException is thrown rather than a
# raw Jackson/SnakeYAML parse exception leaking out of the port.
platform-config:
  apps:
    - name: broken
      current: [type: http
      latest:
        type: http-regex
        url: https://example.test/latest
        regex: '(\d+\.\d+\.\d+)'
