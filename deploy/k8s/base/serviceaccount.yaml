# Dedicated identity for the backend, always — it carries no permissions on
# its own. components/rbac binds the cluster-wide workload-read ClusterRole
# to it, needed only when an app uses the k8s-image current source.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: platformup2date
  labels:
    app.kubernetes.io/name: platformup2date
